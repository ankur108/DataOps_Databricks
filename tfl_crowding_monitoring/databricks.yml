# This is a Databricks asset bundle definition for tfl_crowding_monitoring.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: tfl_crowding_monitoring
  uuid: 4d20f1eb-c0d1-468d-8ccd-9a52fba76baa

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    presets:
      name_prefix: "[dev_${workspace.current_user.short_name}] "
    mode: development
    default: true
    workspace:
      host: https://adb-585476224876066.6.azuredatabricks.net
    variables:
      catalog: dev_tfl_crowding_monitoring
      schema: ${workspace.current_user.short_name}
  staging:
    presets:
        name_prefix: "[staging_${workspace.current_user.short_name}] "
    mode: development
    workspace:
      host: https://adb-238737606032625.5.azuredatabricks.net
    variables:
      catalog: test_tfl_crowding_monitoring
      schema: ${workspace.current_user.short_name}
  prod:
    presets:
      name_prefix: "[prod_${workspace.current_user.short_name}] "
    mode: production
    workspace:
      host: https://adb-1137396238593418.18.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/rattan-bunting0f_icloud.com#ext#@rattanbunting0ficloud.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/rattan-bunting0f_icloud.com#ext#@rattanbunting0ficloud.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prod_tfl_crowding_monitoring
      schema: prod
    permissions:
      - user_name: rattan-bunting0f_icloud.com#ext#@rattanbunting0ficloud.onmicrosoft.com
        level: CAN_MANAGE
